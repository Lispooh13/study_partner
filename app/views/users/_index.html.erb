<table class="table">
   <thead>
     <tr>
         <th>名前</th>
     </tr>
   </thead>
   <tbody>
     <% users.each do |user| %>
       <tr>
         <td>
         <%= link_to user_path(user) do%>
         <%= user.name %>
         <% end %>
         </td>
       </tr>
     <% end %>
   </tbody>
</table>