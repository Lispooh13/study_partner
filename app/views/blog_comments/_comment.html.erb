<!--後で非同期通信実装のため部分テンプレート-->
<%= form_with(model:[blog, blog_comment],local: true)do |f| %>
  <%= f.text_area :comment ,placeholder: "コメントをここに",size: "40", class: "form-control"%>
  <%= f.submit "コメント送信" ,class: "btn btn-sm btn-outline-success "%>
<% end %>